# 每周自动运行 Agent 设计方案

> 本题作答：假设已接入广告平台 API，设计全自动每周分析系统

---

## 一、架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                     调度层（每周一触发）                      │
│          GitHub Actions / Vercel Cron / 云函数              │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                     数据采集层                               │
│                                                             │
│  定时拉取最近 7 天素材级数据                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ Meta API │  │ Google   │  │ TikTok   │  ← 统一的适配器  │
│  │ (Graph)  │  │ Ads API  │  │ Ads API  │     接口         │
│  └──────────┘  └──────────┘  └──────────┘                  │
│                                                             │
│  输出统一格式：                                              │
│  {素材ID, 类型, 展示量, 点击率, 安装成本, 付费人数, 收入}    │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                     分析引擎                                 │
│                                                             │
│  输入：统一格式的 JSON 数据                                   │
│                                                             │
│  Step 1 ─ 字段标准化                                         │
│   不同平台字段名、货币单位、时间格式统一                      │
│                                                             │
│  Step 2 ─ 指标计算                                           │
│   安装量 = 展示量 × 点击率                                    │
│   总花费 = 安装量 × 安装成本                                  │
│   ROI = 7日收入 ÷ 总花费 × 100%                              │
│   付费率 = 付费人数 ÷ 安装量 × 100%                           │
│   ARPPU = 7日收入 ÷ 付费人数                                  │
│                                                             │
│  Step 3 ─ 规则引擎                                           │
│   ┌────────────────────────────────────────────────┐        │
│   │ if ROI >= 15%  → 追投                          │        │
│   │ if 5% <= ROI < 15% → 保留                      │        │
│   │ if ROI < 5% → 暂停                             │        │
│   │ 交叉验证：付费率 > 同类均值 × 1.2 → 强烈追投     │        │
│   │ 交叉验证：ARPPU >= $60 → 高价值保留              │        │
│   └────────────────────────────────────────────────┘        │
│                                                             │
│  Step 4 ─ 类型归因                                           │
│   按 6 种素材类型汇总，识别模式和异常                          │
│                                                             │
│  Step 5 ─ 环比对比                                           │
│   对比上周同期的 ROI 变化、新素材 vs 老素材表现                │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                     输出层                                   │
│                                                             │
│  报告格式：Markdown / HTML                                  │
│  投送渠道：Slack / 邮件 / 飞书 / 钉钉                        │
│  附件：完整数据表 CSV                                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、各模块详细设计

### 2.1 调度层

| 方案 | 优点 | 缺点 |
|---|---|---|
| **GitHub Actions** | 免费、配置简单、日志可查 | 需 GitHub 账号 |
| **Vercel Cron Jobs** | 和你的部署在同一平台 | Pro 计划才支持(20美金/月) |
| **云函数 SCF** | 灵活可控 | 需额外管理 |
| **Cowork 定时任务** | 零部署、自带通知 | 无法直接调用外部 API |

**推荐方案：GitHub Actions**
```yaml
# .github/workflows/ad-analysis.yml
name: 每周素材分析
on:
  schedule:
    - cron: '0 9 * * 1'   # 每周一 09:00 UTC
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: node run-analysis.js
        env:
          META_ACCESS_TOKEN: ${{ secrets.META_TOKEN }}
          META_AD_ACCOUNT_ID: ${{ secrets.AD_ACCOUNT_ID }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}
```

### 2.2 数据采集层 — API 接入方式

**Meta Ads API（示例）：**
```
GET /v21.0/{ad-account-id}/ads?fields=
  ad_id,
  ad_name,
  impressions,
  ctr,
  spend,
  cost_per_install,
  actions(action_type=purchase,time_range={'last_7d'})
```

**字段映射表（统一化）：**

| Meta Ads 字段 | Google Ads 字段 | 统一字段 |
|---|---|---|
| `ad_name` | `ad_group_ad.ad.name` | 素材ID |
| `impressions` | `metrics.impressions` | 展示量 |
| `ctr` | `metrics.ctr` | 点击率 |
| `cpi` | `metrics.cost_per_conversion` | 安装成本 |
| `actions[].7d_purchase` | `metrics.conversions` | 付费人数 |
| `action_values[].7d_purchase` | `metrics.conversions_value` | 付费金额 |

**统一输出接口（所有平台适配后输出相同结构）：**
```typescript
interface AdCreativeData {
  id: string;
  type: '真人剧情' | '游戏实录' | '动画CG' | 'UGC风格' | '静态图片' | '动态图片';
  impressions: number;
  ctr: number;       // 百分比数值，如 3.2
  installCost: number;  // USD
  payers7d: number;
  revenue7d: number;    // USD
  platform: string;     // 数据来源
  dateRange: { start: string; end: string };
}
```

### 2.3 分析引擎 — 规则引擎设计

用配置文件将决策逻辑与代码解耦，方便非技术人员修改阈值：

```json
{
  "rules": {
    "invest": { "min_roi": 15, "label": "追投" },
    "retain": { "min_roi": 5, "max_roi": 15, "label": "保留" },
    "pause":  { "max_roi": 5, "label": "暂停" }
  },
  "crossValidation": {
    "strongInvest": { "payerRateMultiplier": 1.2 },
    "retainForRetarget": { "minArppu": 60 },
    "insufficientSample": { "minImpressions": 50000 }
  },
  "typeClassification": {
    "真人剧情": ["真人", "实拍", "剧情", "演员"],
    "游戏实录": ["实录", "录屏", "gameplay", "实机"],
    "动画CG": ["CG", "动画", "3D", "特效"],
    "UGC风格": ["UGC", "用户", "自拍", "竖屏", "素人"],
    "静态图片": ["图片", "静态", "banner"],
    "动态图片": ["动效", "动态图", "GIF"]
  }
}
```

类型自动分类规则：根据素材 ID 或素材名称中的关键词匹配。如果无法自动匹配，则标记为"未分类"并提示人工标注。

### 2.4 异常处理

| 异常场景 | 处理方式 |
|---|---|
| API 调用失败（限流） | 指数退避重试 3 次，仍失败则跳过该平台 |
| API 返回空数据 | 记录日志，报告标注"该周期无数据" |
| 字段缺失（如无付费数据） | 用可用字段计算，标注"部分指标缺失" |
| 数据波动过大（ROI 环比变化 > 50%） | 自动标记波动标记，建议人工复核 |
| 货币单位不一致 | 读取账户配置中的货币设置，统一转换为 USD |

### 2.5 输出层 — 报告样例

报告投送到 Slack，内容如下：

```
📊 广告素材周报 — 2026/05/18 - 2026/05/24

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📈 整体概览
• 活跃素材：23 个（新增 5 个，暂停 3 个）
• 总花费：$42,580
• 总收入：$8,924
• 整体 ROI：21.0%（↑ 3.2% vs 上周）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 建议追投（4 个）
┌────────┬────────┬──────┬────────┬──────────┐
│ 素材   │ 类型   │ ROI  │ 付费率 │ 建议增幅 │
├────────┼────────┼──────┼────────┼──────────┤
│ V-004  │ UGC    │ 35.5%│ 1.29%  │ +80%    │
│ V-001  │ 真人   │ 14.5%│ 0.99%  │ +30%    │
└────────┴────────┴──────┴────────┴──────────┘

⚠️ 建议暂停（3 个）
• I-001（静态图）ROI 1.0% → 立即暂停
• V-003（动画CG）ROI 2.1% → 存在虚假点击
  （CTR 4.5% 但付费率仅 0.22%）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔮 类型趋势
UGC风格 ROI 35.5% → 主力方向
真人剧情 ROI 8.5% → 保留优化
图片素材 ROI < 5% → 建议全面暂停

💡 下周建议
1. 追制 5 条 UGC 新素材
2. 优化 V-003 的落地页匹配
3. 真人剧情参考 V-001 经验改版 V-005

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
*报告由广告素材分析 Agent 自动生成*
```

---

## 三、评估这道题的得分点

从批卷角度，这道题考察的是：

| 得分点 | 是否覆盖 |
|--------|---------|
| 知道数据从哪里来（API 接入） | ✅ |
| 能自动化触发（定时调度） | ✅ |
| 能处理多平台数据（字段映射） | ✅ |
| 有清晰的判断逻辑（规则引擎） | ✅ |
| 考虑了异常情况（容错机制） | ✅ |
| 输出能被决策者直接用（报告投送） | ✅ |
| 架构模块划分合理（3层结构） | ✅ |
